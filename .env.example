# Database (SQLite for dev, PostgreSQL for production)
DATABASE_URL="file:./dev.db"
# DATABASE_URL="postgresql://user:password@host:5432/palermo_cafe"

# JWT Secret - REQUIRED! Generate with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Palermo Café"

# Admin seed (optional — generates random password if not set)
# ADMIN_EMAIL="admin@palermocafe.pe"
# ADMIN_PASSWORD=""

# Production-only settings (configure in Vercel):
# ALLOWED_ORIGINS="https://palermocafe.pe"
# UPSTASH_REDIS_REST_URL="https://xxx.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="AXxx..."
